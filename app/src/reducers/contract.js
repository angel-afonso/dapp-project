const initialState = {
    web3: null,
    ipfs: null,
    storage: null,
};

export default function (state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}